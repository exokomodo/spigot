node_modules/
build/
dist/
*.lock

# Vendored third-party builds are kept byte-for-byte as published so their
# checksums stay comparable against upstream. Formatting one would de-minify it
# and silently change what ships.
public/vendor/
