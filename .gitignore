# Dependencies
node_modules/

# Build
build/
dist/
tmp/

# TypeScript
*.tsbuildinfo

# Environment
.env
*.env.local

# Editor
.DS_Store
*.log
.vscode/
*.swp
*.swo

# Database
*.sqlite

# Claude Code agent worktrees. Scratch checkouts of this same repo, so a
# `git add` from the root records them as submodule gitlinks. Only the
# worktrees are ignored: settings under .claude/ are worth sharing.
.claude/worktrees/
